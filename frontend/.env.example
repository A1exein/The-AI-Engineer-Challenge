# Backend API base URL. Use this when running the frontend separately from the API.
# Local: backend runs on port 8000 (uv run uvicorn api.index:app --reload)
# Vercel: set this in the Vercel project to your deployed API URL.
NEXT_PUBLIC_API_URL=http://localhost:8000
